{
  "name": "api",
  "version": "0.1.0",
  "description": "<server> npm i\r <client> npm i\r <client> npm run build\r <server> npm run start",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "npm --prefix ../app run build",
    "dev": "npm --prefix ../app run build && nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.10.0",
    "vite": "^4.3.9"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
