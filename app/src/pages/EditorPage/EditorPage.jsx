import React from "react";

import EditorHub from "../../components/editor-hub/EditorHub";

/**
 * Renders the editor page component.
 * @returns {JSX.Element} The rendered editor page component.
 */
function EditorPage() {
    return <EditorHub />;
}

export default EditorPage;
