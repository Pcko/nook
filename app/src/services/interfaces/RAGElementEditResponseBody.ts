export interface RAGElementEditResponseDTO {
    think: string;
    text: string;
    component: string;
    styles: [];
    total_duration: number;
}

export default RAGElementEditResponseDTO